# Copy to .env.production.local and supply your MapTiler browser API key.
# VITE_ values are public in the built website; this is not a server secret.
# Restrict the key's Allowed HTTP origins in MapTiler to your site's domain.
VITE_MAPTILER_KEY=
